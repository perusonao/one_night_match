# Technique Deck Rules — 未決定事項一覧

- ステータス: Phase 1（データモデル基盤）完了時点の一覧（実装が進むにつれて追記・解決していく）
- 関連: [`technique_deck_rules.md`](../rules/technique_deck_rules.md) /
  [`technique_deck_implementation_plan.md`](technique_deck_implementation_plan.md)

各項目には「関連章」「候補（あれば）」「解決が必要なPhase」を記す。ここに
記載された項目は、担当者・プレイテスト結果を踏まえて別途決定し、決定後は
本ファイルから「解決済み」セクションへ移動して仕様書へ反映すること。

---

## 1. 連続攻撃ダメージの即時適用か一括適用か

- 関連章: 仕様書 7.3
- 候補: (a) 即時適用（技成立のたびにHP減算） / (b) 一括適用（連続攻撃終了時にまとめて減算）
- 参考: 現行 `energy` モードの `_land()` は即時適用方式。整合性の観点では(a)が
  有力だが、正式決定はしない。
- 解決が必要なPhase: Phase 5

## 2. 複数のフォール・ギブアップ効果が同一連続攻撃中に成立した場合の処理

- 関連章: 仕様書 7章
- 候補: 未提示（要検討）。最初に成立した効果のみ有効とする案、両方を順に処理する案、
  後から成立した効果で上書きする案などが考えられる。
- 解決が必要なPhase: Phase 6

## 3. HP0時の行動制限

- 関連章: 仕様書 14章
- 候補:
  1. 通常どおり行動可能
  2. 休息しか選べない
  3. 低コスト技のみ使用可能
  4. 返技不可
  5. 自分のターン開始時に強制休息
- 解決が必要なPhase: Phase 3（最小実装／フラグ制で暫定対応し、Phase 6〜7で
  フォール・ギブアップとの相互作用を見て正式決定する）

## 4. 捨て札も空の状態で山札切れした場合の処理

- 関連章: 仕様書 13章
- 候補: 未提示。疲労状態への移行、行動不能ターンとして処理、などが考えられる。
  ただし新モードでは山札切れによる疲労ダメージ自体を採用しない方針（13章）のため、
  「手詰まり」状態の扱いとして別途検討が必要。
- 解決が必要なPhase: Phase 4〜5

## 5. 通常技・固有技・防御カードの同名上限

- 関連章: 仕様書 4章
- 候補: 未提示。フィニッシャー（最大3枚・同名不可）と同様の枚数制限を課すか、
  通常技のみ多投可能とするかなど。
- 解決が必要なPhase: Phase 2

## 6. 特殊キックアウトカードと通常キックアウトカードの区分

- 関連章: 仕様書 4章・9章
- 候補: 未提示。フィニッシャー用の「特殊キックアウトカード」と、通常のフォールに
  対する「キックアウトカード」を別種として区別するか、同一カードプールとするか。
- 解決が必要なPhase: Phase 2・6

## 7. ギブアップ時のHP消費方式

- 関連章: 仕様書 10章
- 候補: (a) 固定値 / (b) 最大HP割合 / (c) 現在HP割合
- 解決が必要なPhase: Phase 6

## 8. 休息の連続使用制限

- 関連章: 仕様書 12章
- 候補: 未提示。現時点の暫定仕様は「連続休息は禁止しない」（12章）。制限を
  課す場合の具体的なルール（例: 同一試合中の使用回数上限）は未検討。
- 解決が必要なPhase: Phase 3

## 9. フィニッシャーに対する返技エネルギーと特殊キックアウトの処理順

- 関連章: 仕様書 8.3章
- 候補: 未提示。防御側がどちらを先に選択できるか、両方を組み合わせられるかなど。
- 解決が必要なPhase: Phase 7

## 10. エスケープカードとリバーサルカードの違い

- 関連章: 仕様書 8.2章
- 候補: 未提示。両者ともフィニッシャー発動キャンセルに使える点は共通だが、
  コスト・追加効果（例: リバーサルは反撃ダメージを伴う等）に差を設けるかは
  未検討。
- 解決が必要なPhase: Phase 7

## 11. HEATの上限と、新モードでの役割

- 関連章: 仕様書 全般（HEATは現行 `energy` モードでレベル解放条件・フィニッシャー
  発動条件・スコアリングに使われる共有リソース）
- 候補: 未提示。上限を設けるか無制限とするか、新モードでのHEATの用途
  （レベル解放条件のみか、フィニッシャー発動条件にも使うか）を検討する必要がある。
- 解決が必要なPhase: Phase 1（データモデル）〜Phase 7（フィニッシャー発動条件）

## 12. レベルシステムを新モードで維持するか

- 関連章: 仕様書 全般
- 候補: 未提示。現行のレベル1〜3（解放条件付き）システムをそのまま流用するか、
  新モードでは技カードの`minimumLevel`のみで管理し、明示的な「レベル変更」
  アクションを廃するかなど。
- 解決が必要なPhase: Phase 1

## 13. 技カード使用後のHEAT処理

- 関連章: 仕様書 6章
- 候補: 未提示。現行モードの`heatDelta`加算方式を踏襲するか、新たな計算式を
  設けるか。
- 解決が必要なPhase: Phase 4

## 14. 1回の連続攻撃中に同名技を複数回使えるか

- 関連章: 仕様書 7.2章
- 候補: 未提示。デッキ内の同名枚数制限（5番）と連動する論点。デッキに複数枚
  投入できるなら物理的に複数回使用が可能になるため、上限設計と合わせて検討。
- 解決が必要なPhase: Phase 5（5番の決定後）

## 15. 手札上限

- 関連章: 仕様書 全般
- 候補: 未提示。
- 解決が必要なPhase: Phase 1〜2

## 16. マリガン

- 関連章: 仕様書 全般
- 候補: 未提示。初期手札が事故った場合の引き直しルールを設けるか。
- 解決が必要なPhase: Phase 2

## 17. 初期手札枚数

- 関連章: 仕様書 全般
- 候補: 未提示。現行 `energy` モードは開始時5枚ドロー
  （`LevelMatchEngine.create()`内の初期ハンド処理）だが、新モードで同一にするかは
  未検討。
- 解決が必要なPhase: Phase 2

## 18. 先攻1ターン目の攻撃可否

- 関連章: 仕様書 5章（ターン進行）
- 候補: 未提示。先攻1ターン目からフルアクション可能とするか、何らかの制限
  （例: エネルギーセットのみ）を設けるかはカードゲーム全般で論点になりやすい
  部分であり、要検討。
- 解決が必要なPhase: Phase 4

## 19. allowedWrestlerIdsが空リストの場合の解釈

- 関連章: 仕様書 6章・7章、実装: `TechniqueDeckTechniqueCard.allowedWrestlerIds`
- 候補: (a) 空リスト＝全レスラー使用可能 / (b) 空リスト＝どのレスラーも
  使用不可（要明示指定）
- 参考: Phase 1では値の保持のみ行い、意味の確定はデッキ検証を実装する
  Phase 2まで先送りした（Phase 1実装指示で明示的に指定された方針）。
- 解決が必要なPhase: Phase 2

## 20. TechniqueTargetStateに自分側の状態条件を追加する必要性

- 関連章: 仕様書 11.3章、実装: `TechniqueTargetState`
- 候補: 未提示。現在の `TechniqueTargetState` は相手の状態（`any`/`stand`/
  `down`）のみを表す。「自分がダウン中のみ使用可能」等、自分側の状態を
  条件とする技が今後必要になった場合、別enum・別フィールドの追加が要る。
- 参考: Phase 0仕様書に明記がないため、Phase 1では先行実装しなかった。
- 解決が必要なPhase: Phase 3（スタンド／ダウン実装時に要否を再検討）

---

## 解決済み

### A. 新ルールモード識別子は独立enumとする（`MatchResourceMode`は拡張しない）

- 関連: 実装計画Phase 1
- 決定内容: `TechniqueDeckResourceMode { disabled, techniqueDeck }` を新設し、
  既存 `MatchResourceMode`（classic/energy）へは追加しない。
- 理由: `MatchResourceMode` は `LevelMatchEngine`/`LevelMatchState` 内の
  数十箇所の網羅的switchで使われており、3値目を追加すると既存分岐にも
  影響するリスクがある。独立させることで既存コードへの影響をゼロに保てる。
- 解決日: Phase 1実装時。実戦エンジンへ接続する段階（Phase 4以降）で
  正式統合するかは改めて判断する。

### B. 技カード種別は独立enumとする（`MoveCategory`は拡張しない、案B採用）

- 関連: 実装計画Phase 1、仕様書3章
- 決定内容: `TechniqueCardCategory { normal, signature, finisher }` を
  新設し、既存 `MoveCategory`（normal/counter/finisher/basic）へは追加しない。
- 理由: 既存 `MoveCategory` はclassic/energyモードの実装・テスト資産と
  強く結び付いており、Technique Deck専用の分類を混在させると将来的な
  分岐の混乱を招くため（案A: 拡張／案B: 新設 のうち案Bを採用）。
- 解決日: Phase 1実装時。
